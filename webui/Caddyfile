{
	admin off
	auto_https off
}

# Railway injects $PORT; fall back to 8080 for local `docker run`.
:{$PORT:8080} {
	root * /srv
	file_server
	# The landing page is the front door; anything unmatched lands there.
	try_files {path} {path}.html /index.html
	encode gzip
}
